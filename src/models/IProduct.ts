export interface IProduct {
  id: number;
  image: string;
  title: string;
  grams: string;
  subtitle: string;
  price: string;
  wishList: boolean;
  shoppingCart: boolean;
  quantity: number;
  category: string;
}
